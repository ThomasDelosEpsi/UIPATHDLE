import React from 'react'
import ReactDOM from 'react-dom/client'
import UiPathdle from './UIPATHDLE'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <UiPathdle />
  </React.StrictMode>,
)